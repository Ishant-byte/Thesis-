{
  "name": "pramaanhrms",
  "private": true,
  "version": "1.0.0",
  "description": "PKI-secured HRMS coursework project with FastAPI, MongoDB, and React.",
  "scripts": {
    "install:all": "npm install --prefix web",
    "dev": "powershell -ExecutionPolicy Bypass -File scripts/dev.ps1",
    "web": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "preview": "npm run preview --prefix web",
    "test": "python -m pytest -q",
    "server": "python -m server.app",
    "client": "python -m client.main"
  }
}
